let n = [10, 25, 7, 40, 18];// Define an array with at five numerical values
let s = 0;// Variable to store the sum
for (let i = 0; i < n.length; i++) // Loop through the array and add each value to the sum
{
    s += n[i];
}
console.log("The sum : " + s);// Display the final result

